# =============================================================================
# AtcoGenie AI Engine — Python Dependencies
# Pin all versions for reproducibility
# =============================================================================

# --- Core Framework ---
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# --- Excel + Data Processing ---
pandas>=2.0.0           # Excel → Parquet conversion in upload pipeline
pyarrow>=14.0.0         # Parquet read/write engine used by pandas

# --- LangChain 1.0 ---
langchain>=0.3.0
# langchain-openai>=0.3.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
langgraph-checkpoint-postgres>=2.0.0

# --- Database Drivers ---
aioodbc
# hdbcli>=2.23.24         # SAP HANA (commented out as not needed yet)
sqlparse
asyncpg
psycopg-binary

# --- Excel Upload & In-Process Query ---
duckdb>=0.10.0          # In-process analytical queries on user-uploaded Parquet files
openpyxl>=3.1.0         # pandas engine for .xlsx
xlrd>=2.0.1             # pandas engine for legacy .xls
tabulate>=0.9.0         # df.to_markdown() for DuckDB query results

# --- Authentication & Security ---
ldap3==2.9.1            # Active Directory LDAP authentication
PyJWT                  # JWT issuance and validation (standard)
passlib                # Password hashing (standard)

# --- Caching & Session ---
redis                  # Redis client (standard)
langchain-google-genai # Google Gemini support (as requested)

# --- Observability ---
structlog==25.1.0       # Structured logging
langfuse>=2.0.0,<3.0.0  # Pinned v2 to match langfuse:2 Docker server. SDK not used directly (Python 3.14 pydantic v1 compat issue) — raw HTTP tracer in app/agent/tracer.py instead
# langsmith==0.3.45       # [Temporarily disabled - needs MSVC build tools on 3.14]

# --- SSE Streaming ---
sse-starlette==2.3.3    # Server-Sent Events for FastAPI

# --- Testing ---
pytest
pytest-asyncio
pytest-cov
httpx           # Async test client for FastAPI

# --- Code Quality ---
# black==25.1.0          # [Temporarily disabled - needs compilation on 3.14]
ruff==0.11.4
mypy==1.15.0
